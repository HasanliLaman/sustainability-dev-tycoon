import styled from "styled-components";

const StyleTutorial = styled.section`
  article {
    margin-top: 2.4rem;
    display: flex;
    flex-direction: column;
    gap: 2.4rem;
    font-weight: var(--fw-regular);
  }
`;

export default StyleTutorial;
